#include "../include/luckydog.h"
#include <conio.h>

int main(){
	App app;
	app.get_result();
	//for (int i = 0; i < 10; i++) {
	//	std::cout<<app.result_list[i]<<std::endl;
	//}
	getch();
}